/**
 * @deprecated
 * No longer used. The create-job modal flow now lives in `CreateJobModal` and uses `useCreateJob` + `useSaveStatus`.
 */

export default function useCreateJobMutation(): never {
  throw new Error("useCreateJobMutation is deprecated. Use CreateJobModal + useCreateJob/useSaveStatus instead.");
}
